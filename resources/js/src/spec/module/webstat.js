/**
 * A test file for  WebStat 
 */

import WebStat from '../../modules/webstat.js';

describe('WebStat Class', () => {

	var webstat, console, driver, hostname, port, path;

	

	it('Simple Test', () => {
		
		var webstat = new WebStat(console);

	
	});


});

